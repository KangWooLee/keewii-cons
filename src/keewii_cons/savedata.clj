(ns keewii_cons.savedata
  
  (:use [keewii_cos.toolbox])
        ) ;f1,f2?? [keewiiVisual.basic_setting :only [F1 F2]




;temp-path (str (cur_dir) "\\temp\\")
;        recording-info (saveData/save-temp-wav sox-path temp-path session-number)
;        data-path (init/datapath @all-options)
;(sox-recorder recording-info)
;(saveData/save-temp-seq temp-path session-number) ; save current alphabet (right answer)
;(saveData/save-data-file temp-path data-path session-number)

;(defn save-data-file [path-temp path-data session-number]
;  (let []
;    (copy-file (str path-temp (format "%02d" session-number) ".wav") (str path-data "\\wav\\" (format "%02d" session-number) ".wav"))
;    (copy-file (str path-temp (format "%02d" session-number) ".txt") (str path-data "\\dat\\" (format "%02d" session-number) ".dat")))) 

